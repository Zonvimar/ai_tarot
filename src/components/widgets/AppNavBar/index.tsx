import NavBar from "@/components/widgets/NavBar";
import {Suspense} from "react";


const AppNavBar = () => {
    return (
        <Suspense>
            <NavBar/>
        </Suspense>
    )
}

export default AppNavBar